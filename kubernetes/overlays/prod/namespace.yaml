apiVersion: v1
kind: Namespace
metadata:
  name: sequence-prod
  labels:
    istio-injection: enabled          # Enable Istio sidecar injection
    pod-security.kubernetes.io/enforce: restricted  # Enforce K8s Pod Security Standards
    pod-security.kubernetes.io/warn: restricted
    environment: prod
